% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{fetch_prices}
\alias{fetch_prices}
\title{Fetch Market Prices from Multiple Sources}
\usage{
fetch_prices(
  tickers,
  from = Sys.Date() - 365,
  to = Sys.Date(),
  source = "yahoo",
  interval = "daily",
  auto_adjust = TRUE,
  api_key = NULL,
  ...
)
}
\arguments{
\item{tickers}{Character vector: one or more ticker symbols (e.g., "AAPL", "BTC-USD")}

\item{from}{Date or character: start date (default: 1 year ago)}

\item{to}{Date or character: end date (default: today)}

\item{source}{Character: data source - "yahoo" (default), "alpha_vantage", or custom}

\item{interval}{Character: time interval - "daily", "1hour", "1min", etc. (default: "daily")}

\item{auto_adjust}{Logical: automatically adjust for splits/dividends (default: TRUE)}

\item{api_key}{Character: API key for sources that require it (e.g., Alpha Vantage)}

\item{...}{Additional arguments passed to source-specific functions}
}
\value{
A \code{market_tbl} object with columns: symbol, datetime, open, high, low, close, volume, adjusted
}
\description{
Universal function to fetch OHLCV market data from various sources.
Automatically detects the best source and returns normalized \code{market_tbl} data.
}
\examples{
\dontrun{
# Fetch single stock
aapl <- fetch_prices("AAPL", from = "2024-01-01")

# Multiple stocks
tech <- fetch_prices(c("AAPL", "MSFT", "GOOGL"), from = "2023-01-01")

# Cryptocurrency
btc <- fetch_prices("BTC-USD", from = "2024-01-01")

# Using Alpha Vantage
prices <- fetch_prices(
  "AAPL",
  source = "alpha_vantage",
  api_key = Sys.getenv("ALPHA_VANTAGE_KEY")
)
}
}
