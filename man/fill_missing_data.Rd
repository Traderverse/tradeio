% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{fill_missing_data}
\alias{fill_missing_data}
\title{Fill Missing Data}
\usage{
fill_missing_data(
  data,
  method = c("forward", "backward", "linear", "spline"),
  columns = NULL
)
}
\arguments{
\item{data}{A \code{market_tbl} object}

\item{method}{Character: "forward" (last observation carried forward),
"backward" (next observation carried backward), "linear" (linear interpolation),
or "spline" (spline interpolation)}

\item{columns}{Character vector: columns to fill (default: all numeric columns)}
}
\value{
A \code{market_tbl} with filled values
}
\description{
Fill missing values in market data using various methods.
}
\examples{
\dontrun{
data <- fetch_prices("AAPL", from = "2024-01-01")

# Forward fill
filled <- fill_missing_data(data, method = "forward")

# Linear interpolation
filled <- fill_missing_data(data, method = "linear")
}
}
